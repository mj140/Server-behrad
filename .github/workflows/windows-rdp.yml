name: Permanent Windows RDP Server

on:
  schedule:
    - cron: '0 */6 * * *'  # اجرای دقیق هر ۶ ساعت
  workflow_dispatch:        # قابلیت استارت دستی

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360 # سقف ۶ ساعت گیتهاب

    steps:
      - name: 1. Setup Admin Account
        run: |
          $Password = ConvertTo-SecureString "Cynet@2026" -AsPlainText -Force
          New-LocalUser -Name "CynetAdmin" -Password $Password -PasswordNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "CynetAdmin" -ErrorAction SilentlyContinue
          net user runneradmin Cynet@2026

      - name: 2. Enable English & Persian Keyboards
        run: |
          $LangList = New-WinUserLanguageList -Language "en-US"
          $LangList.Add("fa-IR")
          Set-WinUserLanguageList -LanguageList $LangList -Force

      - name: 3. Enable RDP & Firewall
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 0

      - name: 4. Connect Tailscale VPN
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
        run: |
          Invoke-WebRequest "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "C:\tailscale.msi"
          Start-Process msiexec -ArgumentList "/i C:\tailscale.msi /quiet /norestart" -Wait
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey="$Env:TAILSCALE_AUTHKEY" --hostname="github-rdp-server"
          $ts_ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          Write-Host "Tailscale IP: $ts_ip" -ForegroundColor Yellow

      - name: 5. Keep Alive (5 Hours 55 Minutes)
        run: |
          for ($i = 1; $i -le 355; $i++) {
            Start-Sleep -Seconds 60
            Write-Host "Server Active - Time elapsed: $i minutes"
          }
          
